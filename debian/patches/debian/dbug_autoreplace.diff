From: Modestas Vainius <modestas@vainius.eu>
Subject: [PATCH] debian/dbug_autoreplace

Add a default autoreplace which replaces dbug:bugnumber with
the respective Debian BTS URL.

Signed-off-by: Modestas Vainius <modestas@vainius.eu>

---
 src/config/preferences.cpp |    1 +
 1 files changed, 1 insertions(+), 0 deletions(-)

diff --git a/src/config/preferences.cpp b/src/config/preferences.cpp
index 7080185..9402986 100644
--- a/src/config/preferences.cpp
+++ b/src/config/preferences.cpp
@@ -127,6 +127,7 @@ void Preferences::clearQuickButtonList()
 const QStringList Preferences::defaultAutoreplaceList()
 {
     return QStringList() << "1,o,\\[\\[([^\\s]+)\\]\\],http://en.wikipedia.org/wiki/Special:Search?go=Go&search=%1"
+                         << "1,o,([Dd][Bb][Uu][Gg]:)(\\w+),http://bugs.debian.org/%2"
                          << "1,o,(BUG:|bug:)([0-9]+),http://bugs.kde.org/show_bug.cgi?id=%2";
 }
 
-- 
tg: (15963f5..) debian/dbug_autoreplace (depends on: upstream)
